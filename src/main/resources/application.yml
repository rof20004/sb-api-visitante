spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:visitantes
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
      path: /h2-console
  sql:
    init:
      platform: h2